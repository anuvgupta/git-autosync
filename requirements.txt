# Optional. sync.py runs on stdlib alone — but if `filelock` is available it
# is preferred as the file-lock backend (unifies Unix/Windows behavior and
# handles edge cases like NFS more carefully than raw fcntl/msvcrt).
#
# Install with:  pip install -r requirements.txt
filelock>=3.0
